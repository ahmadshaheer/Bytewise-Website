---
import { withBase } from "../utils/withBase";

interface Props {
  html: string;
}

const { html } = Astro.props;
const content = withBase(html, import.meta.env.BASE_URL);
---

<div set:html={content} />
